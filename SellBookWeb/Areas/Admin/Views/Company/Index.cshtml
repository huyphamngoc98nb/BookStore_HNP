@model List<Company>

<div class="container">
    <div class="row">
        <div class="col-6 pb-2">
            <h2 class="text-primary">Company List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Company" asp-action="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Create New Company
            </a>
        </div>
        <table id="tblData" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        Name
                    </th>
                    <th>
                        State
                    </th>
                    <th>
                        City
                    </th>
                    <th>
                        Phone Number
                    </th>
                    <th></th>
                </tr>
            </thead>
            @* <tbody>
            @foreach (var obj in Model)
            {
            <tr>
            <td>@obj.Title</td>
            <td>@obj.Author</td>
            <td>@obj.Category.Name</td>
            <td>@obj.ListPrice</td>
            <td>
            <div class="w-100 btn-group" role="group">
            <a asp-action="Upsert" asp-controller="Product" asp-route-id="@obj.Id" class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i>Edit</a>

            <a asp-action="Delete" asp-controller="Category" asp-route-id="@obj.Id" class="btn btn-danger mx-2"><i class="bi bi-trash"></i>Delete</a>

            <form asp-action="Delete" asp-controller="Product" asp-route-id="@obj.Id" method="post" onsubmit="return confirmDelete();">
            <button type="submit" class="btn btn-danger mx-2"><i class="bi bi-trash"></i>Delete</button>
            </form>

            </div>
            </td>
            </tr>
            }
            </tbody> *@
        </table>

    </div>
</div>

<script>

    function confirmDelete() {
        return confirm("Are you sure you want to delete this company?");
    }
</script>

@section Scripts {
    <script src="~/js/company.js">
    </script>
}